package natan.devteste.digionebank.testes

import natan.devteste.digionebank.Logavel

// cria a classe TesteAutenticacao
class TesteAutenticacao {
    fun autentica(logavel: Logavel) = println(logavel.Login()) // função de login
}